# Webアプリ仕様書

## 1. 概要

入力されたテキストをLLMを用いて要約、表示するアプリ。  
入力にはテキスト入力のほか、音声入力が使用できる。
出力には複数のフォーマット（例: 箇条書き・レポート風・小説風）からスタイルを選択できる。

### 処理

このアプリは、入力の準備と要約生成の2段階で処理を行う。

#### 1. 入力の準備

- 入力：音声入力
- 出力：音声入力を文字起こし

#### 2. 要約の生成

- 入力：要約のスタイルを選択
- 出力：要約の型に当てはめて表示

## 2. 主な機能

### 必須の機能

- [x] 音声入力を文字起こし
- [x] 要約の仕方を選択し、要約テンプレに当てはめる
- [x] 要約当てはめのやり直し

### 余裕があれば作る機能

- [x] 要約内容のダウンロード
- [ ] 要約からカレンダーの予定を作成

### 🚧実装状況

- **フロントエンド実装**:
  - [x] Vue Routerによるマルチビュー（Home / History / About）
  - [x] MediaRecorderによる音声録音・タイマー
  - [x] 要約スタイル切り替え
  - [x] 要約モデル選択UI
  - [x] LocalStorageによる履歴永続化
  - [x] ファイルダウンロード・印刷レイアウト
  - [x] 通知トースト
  - [ ] 内容からカレンダーの予定の生成
- **バックエンドAPI実装**:
  - [x] Flask開発サーバ
    - [x] デバッグモードで開発サーバを起動
    - [x] `frontend/`を`static/`扱いにする
    - [x] `http://localhost:5000/`を`/static/index.html`に転送する
  - [x] `/api/transcribe`: 音声受付・文字起こし
    - [x] `services/transcription.py`: faster-whisperによる音声文字起こし
  - [x] `/api/summarize`: 文章の要約
    - [x] Ollamaへのリクエストと結果受け取り
    - [x] 複数の要約スタイルでプロンプトを生成
    - [x] 要約をストリームで送る
    - [x] 使用モデル指定パラメータ（`model`）対応
  - [x] `/api/models`: 利用可能なOllamaモデル一覧取得
  - [x] `/api/summary-types`: テキストファイル定義に基づく要約形式一覧取得
  - [x] `/api/submit`: 要約データ送信受付
    - [ ] `/api/submit`: SQLite等を用いた要約履歴のサーバーサイドでの永続化

### 2.1 入出力テキストエリア

- 入力テキストエリアにユーザーが要約内容対象のテキストを入力できる
- バックエンドに実装されたAPIへリクエストを発行し、LLMで生成された応答を出力テキストエリアに出す
- 入力テキストと同一画面で回答を表示する

### 2.2 生成AI(言語モデル)へのリクエスト API

- ユーザー入力とシステムプロンプトを合成し、LLMに渡す入力コンテキストを生成
  - 必要ならコンテキストを追加し、望ましい回答を得られるように調整する
- LLMに入力コンテキストをAPI越しにリクエストする
  - 方法1(優先): Ollamaでローカルモデルを使用する。Ollama独自のAPI形式(`http://localhost:11434/api`)か、OpenAI互換API形式(`http://localhost:11434/v1`)のどちらかを使用する。いずれもPythonにラッパーあり。
  - 方法2(任意): OpenRouterを使用して各社のクラウドモデル（例：GPT-4, Claude, Geminiなど）を使用する。ユーザ登録とキーの発行が必要。ラッパーあり。
- 応答をJSONで包み、フロントエンドに返す

## 3. 使用技術

| 分類           | 技術・ライブラリ                                                                                                                                                                       |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| フロントエンド | Vue.js / HTML5 + CSS3 / Bootstrap5.3                                                                                                                                                   |
| バックエンド   | Flask / Python                                                                                                                                                                         |
| 通信方式       | http / REST API / JSON                                                                                                                                                                 |
| LLMモデル      | 方法1: Ollama上で0.8b~2b程度の軽量なモデルを動かす。例: `qwen3.5:0.8b` <br>方法2: OpenRouter API経由で任意のモデルを使用する。例: `nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free` |

## 4. 入出力例

### 入力

#### ユーザ入力:

> あー、Webアプリ開発って、えーっと、実際に作ってみると予想もしないことがよく起きるんですよね。  
> そのー、最初は簡単なタスク管理アプリを作ろうとしていたんですよ。フロントエンドはJavaScriptでサクッと書いて、バックエンドはPythonでFlaskを使ってサクッと連携させる予定だったんです。

#### システムプロンプト:

> [指示]  
> 音声入力を元に、次を行なって文章を生成してください  
> ・フィラーを削除  
> ・意味のまとまりで構造化して箇条書きで整理する  
> ・重複した発言をまとめる  
> ・余談や順番が前後してしまった話を分離し、正しい位置にまとめる
>
> [ルール]  
> ・事実や数値を変えない  
> ・想像で内容を足さない

### 出力例

> - Webアプリ開発は予想外のことがよく起こる。
> - タスク管理アプリの開発を予定していた。
> - フロントエンドはJavaScriptで、バックエンドはPythonのFlaskで連携する予定だった。

## 5. 開発フロー

### 大まかな流れ

**(R：れい、K：かいと)**

```text
[Webアプリ開発]
├── 1. 要件定義 & 設計
│     ├── 1.1 画面設計書作成 (担当: R, 2h)
│     └── 1.2 API仕様書作成 (担当: K, 2h)
├── 2. フロントエンド実装
│     ├── 2.1 フォームUI作成 (担当: R, 4h)
│     └── 2.2 API通信連携 (担当: R, 4h)
└── 3. バックエンド実装
      ├── 3.1 Flask API構築 (担当: K, 4h)
      └── 3.2 Ollama連携 (担当: K, 4h)
```

### ガントチャート

#### 第一週: 8/31(月)〜9/4(金)

| タスク名      | 担当者 | Day1 <br> 8/31 | Day2 <br> 9/1 | Day3 <br> 9/2 | Day4 <br> 9/3 | Day5 <br> 9/4 | 進捗 |
| ------------- | ------ | -------------- | ------------- | ------------- | ------------- | ------------- | ---- |
| 1. 要件定義   | R & K  | ■■             | ■■            |               |               |               | 100% |
| 2. 設計       | R & K  |                |               | ■■            |               |               | 100% |
| 3. バック     | K      |                |               |               | ■■            | ■■            | 70%  |
| 4. フロント   | R      |                |               |               | ■■            | ■■            | 20%  |
| 5. 結合テスト | R & K  |                |               |               |               |               | 0%   |
| 6. 成果発表   | R or K | --             | --            | --            | --            | --            | 0%   |

#### 第二週: 9/7(月)〜9/11(金)

| タスク名      | 担当者 | Day6 <br> 9/7 | Day7 <br> 9/8 | Day8 <br> 9/9 | Day9 <br> 9/10 | Day10 <br> 9/11 | 進捗 |
| ------------- | ------ | ------------- | ------------- | ------------- | -------------- | --------------- | ---- |
| 1. 要件定義   | R & K  |               |               |               |                |                 | 100% |
| 2. 設計       | R & K  |               |               |               |                |                 | 100% |
| 3. バック     | K      | ■■            |               |               |                |                 | 100% |
| 4. フロント   | R      | ■■            | ■■            | ▷▷            |                |                 | 95%  |
| 5. 結合テスト | R & K  |               |               |               |                |                 | %    |
| 6. 成果発表   | R or K | ■■            | ■■            | ▷▷            | --             | 〇〇            | 40%  |

### API全体の処理フロー

```text
[フロントエンド]
       │
       │ 音声ファイル + 要約形式
       ▼
POST /api/summary-jobs
       │
       ▼
[バックエンド]
       │
       ├─① 音声認識
       │       ↓
       │   文字起こしテキスト
       │
       ├─② 要約形式を確認
       │       ↓
       │   指定されたテンプレート
       │
       ├─③ LLMによる要約
       │       ↓
       │   要約結果
       │
       ▼
GET /api/summary-jobs/{jobId}
       │
       ▼
[フロントエンド]
       │
       ▼
要約結果を画面に表示
```

## 6. APIについて

### APIエンドポイント一覧

括弧付きは未実装。

| No. | エンドポイント              | HTTPメソッド | 機能                               | 入力                     | 出力           |
| --- | --------------------------- | ------------ | ---------------------------------- | ------------------------ | -------------- |
| 1   | `/api/transcribe`           | POST         | 音声ファイルを文字起こしする       | 音声ファイル             | 文字起こし結果 |
| (2) | `/api/summary-templates`    | GET          | 利用可能な要約形式を取得する       | なし                     | 要約形式一覧   |
| 3   | `/api/summarize`            | POST         | 文字起こし結果を指定形式で要約する | 文字起こし結果・要約形式・モデル(任意) | 要約結果 (ストリーミング) |
| 4   | `/api/models`               | GET          | 利用可能なOllamaモデル一覧を取得する | なし                   | モデル一覧     |
| 5   | `/api/summary-types`        | GET          | 定義済みの要約形式一覧を取得する   | なし                     | 要約形式一覧   |
| (6) | `/api/summary-jobs`         | POST         | 文字起こし～要約までを一括実行する | 音声ファイル・要約形式   | 処理結果       |
| (7) | `/api/summary-jobs/{jobId}` | GET          | 処理状況・結果を取得する           | ジョブID                 | 処理状況・結果 |

### 1. `POST /api/transcribe`

**音声文字起こしAPI**  
→音声ファイルを受け取り、音声認識によってテキストへ変換する

#### 入力

音声データはJSONではなく、`multipart/form-data` を想定
| パラメータ | 型 | 必須 | 内容 |
|-----------|----|------|------|
| audio | File | 〇 | 音声ファイル |
| language | String | △ | 音声の言語 ex:ja |

#### 出力

```JSON
{
  "transcriptionId": "tr_001",
  "text": "本日の会議では、新商品の開発スケジュールについて確認しました。",
  "language": "ja",
  "duration": 125.4,
  "createdAt": "2026-09-02T13:00:00+09:00"
}
```

##### 実際の出力

```JSON
{
  "success": True,
  "text": "本日の会議では、新商品の開発スケジュールについて確認しました。"
}
```

### 2. `GET /api/summary-templates`

**要約形式取得API**  
→ユーザーが選択可能な要約形式を取得する  
→箇条書き、会議議事録、要点3つ、短い要約etc...

#### 入力

なし

#### 出力

```JSON
{
  "templates": [
    {
      "id": "bullet",
      "name": "箇条書き",
      "description": "内容を重要なポイントごとに箇条書きで整理します。"
    },
    {
      "id": "meeting",
      "name": "会議議事録",
      "description": "会議の概要、決定事項、課題、TODOを整理します。"
    },
    {
      "id": "three-points",
      "name": "要点3つ",
      "description": "内容を特に重要な3つのポイントにまとめます。"
    }
  ]
}
```

### `POST /api/summarize`

**要約生成API**  
→文字起こしされたテキストとユーザーが選択した要約形式を受け取り、LLM等を利用して要約する

#### 入力

過去の文字起こしがすでに保存されている場合：

```JSON
{
  "transcriptionId": "tr_001",
  "templateId": "meeting"
}
```

または、文字起こし結果を直接渡す場合：

```JSON
{
  "text": "本日の会議では、新商品の開発スケジュールについて確認しました。",
  "summary_type": "meeting",
  "model": "qwen3.5:0.8b"
}
```

| パラメータ | 型 | 必須 | 内容 |
|-----------|----|------|------|
| text | String | 〇 | 要約対象のテキスト |
| summary_type | String | 〇 | 要約形式（bullet, meeting, report, short等） |
| model | String | △ | 使用するOllamaモデル名（省略時はデフォルトモデルを使用） |

#### 実際の動作仕様 (ストリーミング)

- Content-Type: `text/plain; charset=utf-8`
- レスポンス形式: トークンごとのチャンクストリーミング (`ReadableStream`)
- 生成された要約テキストが順次ストリーミング送信され、フロントエンド側でリアルタイムに画面描画・Markdownレンダリングされる。

#### 出力 (初期設計案)

```JSON
{
  "summaryId": "sum_001",
  "templateId": "meeting",
  "templateName": "会議議事録",
  "summary": {
    "overview": "新商品の開発スケジュールについて確認した。",
    "decisions": [
      "9月末までに試作品を完成させる。"
    ],
    "issues": [
      "開発担当者の人員が不足している。"
    ],
    "todos": [
      {
        "task": "追加の開発担当者を検討する",
        "assignee": "田中",
        "deadline": "2026-09-10"
      }
    ]
  },
  "createdAt": "2026-09-02T13:05:00+09:00"
}
```

### `POST /api/summary-jobs`

**一括処理API**  
今回のシステムでは、このAPIを中心にすると設計しやすい(by chat GPT)  
→音声ファイルのアップロードから、文字起こし、要約までを一括して実行するAPI

#### 入力

`multipart/form-data`を想定する。  
| パラメータ | 型 | 必須 | 内容 |
|------------|----|-----|-----|
| audio | File | 〇 | 音声ファイル |
| templateId | String | 〇 | 使用する要約形式 |
| language | String | △ | 音声言語 |

- 例：
  - **audio**: meeting.mp3
  - **templateId**: meeting
  - **language**: ja

#### 出力

非同期処理のため、まずジョブIDを返す

```JSON
{
  "jobId": "job_001",
  "status": "processing",
  "createdAt": "2026-09-02T13:10:00+09:00"
}
```

その後、`/api/summaries-jobs/{jobID}`にGETリクエストして結果を取得する

### `GET /api/summary-jobs/{jobID}`

**処理状況・結果取得API**

#### 入力

`/api/summary-jobs`で与えられる`jobID`を渡す

#### 出力

##### 処理中

```JSON
{
  "jobId": "job_001",
  "status": "processing",
  "progress": 60
}
```

##### 処理完了時

```JSON
{
  "jobId": "job_001",
  "status": "completed",
  "progress": 100,
  "transcription": {
    "text": "本日の会議では、新商品の開発スケジュールについて確認しました。"
  },
  "summary": {
    "templateId": "meeting",
    "templateName": "会議議事録",
    "content": {
      "overview": "新商品の開発スケジュールについて確認した。",
      "decisions": [
        "9月末までに試作品を完成させる。"
      ],
      "issues": [
        "開発担当者の人員が不足している。"
      ],
      "todos": [
        {
          "task": "追加の開発担当者を検討する",
          "assignee": "田中",
          "deadline": "2026-09-10"
        }
      ]
    }
  },
  "completedAt": "2026-09-02T13:12:00+09:00"
}
```

### `GET /api/models`

**Ollamaモデル一覧取得API**  
→ローカルのOllamaインスタンスで現在利用可能なモデル一覧および詳細情報を取得する。

#### 入力

なし

#### 出力

```JSON
{
  "success": true,
  "models": [
    {
      "model_name": "qwen3.5:0.8b",
      "parameter_size": "0.8B",
      "quantization_level": "Q4_K_M"
    }
  ]
}
```

| フィールド | 型 | 内容 |
|-----------|----|------|
| model_name | String | Ollamaモデル識別子（例: `qwen3.5:0.8b`） |
| parameter_size | String | モデルのパラメータ数（例: `0.8B`, `7B`） |
| quantization_level | String | 量子化レベル（例: `Q4_K_M`） |

### `GET /api/summary-types`

**要約形式一覧取得API**  
→`backend/prompts/` ディレクトリ内のテキストファイル（`*.txt`）に基づいて利用可能な要約形式キーの一覧を取得する。

#### 入力

なし

#### 出力

```JSON
{
  "success": true,
  "summary_types": [
    "bullet",
    "meeting",
    "report",
    "short"
  ]
}
```

